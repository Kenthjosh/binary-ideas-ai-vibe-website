---
const losses = [
  "15-20 leads walk away because you couldn't respond fast enough",
  "$5,000-$10,000 in lost revenue from missed opportunities",
  "Your competitors capture those customers because they respond in 60 seconds",
  "You keep working nights and weekends trying to keep up, missing family time"
];
---

<section class="relative w-full px-4 py-16 md:px-8 lg:px-20 lg:py-24">
  <div class="mx-auto flex max-w-4xl flex-col items-center gap-10 text-center">
    <div class="space-y-4">
      <h2 class="text-3xl md:text-4xl font-semibold [font-family:'Segoe_UI_Symbol-Regular',Helvetica]">
        <span class="text-white">WITHOUT 24/7 </span>
        <span class="text-transparent bg-clip-text bg-[linear-gradient(90deg,#2b7fff_0%,#ac46ff_50%,#f6329a_100%)]">
          AI SUPPORT
        </span>
        <span class="text-white">, HERE'S WHAT YOU'RE LOSING EVERY SINGLE MONTH</span>
      </h2>
    </div>

    <div class="w-full space-y-6">
      {losses.map((loss, index) => (
        <div class="flex flex-col items-center gap-4">
          <div class="w-full rounded-[20px] border border-white/10 bg-[#0f172a]/80 px-6 py-5 text-base leading-7 text-[#d7deed] shadow-[0_20px_60px_rgba(5,8,20,0.45)]">
            {loss}
          </div>
          {index < losses.length - 1 && (
            <div class="text-3xl text-[#6ea8ff]" aria-hidden="true">
              ↓
            </div>
          )}
        </div>
      ))}
    </div>

    <div class="pt-4">
      <a
        class="inline-flex items-center justify-center rounded-[30px] bg-[linear-gradient(90deg,#38bdf8_0%,#6366f1_100%)] px-8 py-4 text-lg font-semibold text-white shadow-[0_25px_60px_rgba(56,189,248,0.35)] transition-transform duration-200 hover:scale-[1.04]"
        href="https://api.3sixtycrm.com/widget/bookings/robert-sandiego"
        target="_blank"
        rel="noopener"
      >
        Stop the Bleeding – Book Your Consultation
      </a>
    </div>
  </div>
</section>

